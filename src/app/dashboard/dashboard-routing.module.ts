import { Routes } from '@angular/router';
import { HomeDashboardComponent } from './home';

export const DashboardRoutes: Routes = [
    {
        path: 'dashboard',
        component: HomeDashboardComponent
      },
];